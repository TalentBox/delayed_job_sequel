source "http://rubygems.org"

gemspec :path => "../"

gem "sequel", "#{ENV["SEQUEL"]}"

# MRI Adapter Dependencies
platform :ruby do
  gem 'pg'
  gem 'mysql2'
  gem 'sqlite3'
end

# JRuby Adapter Dependencies
platform :jruby do
  gem 'jdbc-sqlite3'
  gem 'jdbc-mysql'
  gem 'jdbc-postgres'
end
